name: Transform main

on:
  workflow_dispatch:
  push:
    branches:    
      - main
    paths:
      - 'Frends.XSLT.Transform/**'

jobs:
  build:
    uses: ./.github/workflows/build_main.yml
    with:
      workdir: Frends.XSLT.Transform
    secrets: inherit
      