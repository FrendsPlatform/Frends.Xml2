name: Validate test

on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - 'Frends.XML.Validate/**'

jobs:
  build:
    uses: ./.github/workflows/build_test.yml
    with:
      workdir: Frends.XML.Validate
    secrets: inherit
      